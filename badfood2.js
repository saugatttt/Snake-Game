let badFood;
let badFoodX;
class BadFood{
  
  constructor(){
    this.x = x;
    this.y = y;
    
  }
  
  location(){
    
    let x = floor(random(w));
  let y = floor(random(h));
    let x2 = floor(random(w));
    print(x2);
    let y2 = floor(random(h));
  badFood = createVector(x, y);
   // badFoodX = createVector(x2, y2);
  
  }
  
  locationTwo(){
  
  
  }
}
